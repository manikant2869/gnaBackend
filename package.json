{
  "name": "serverside",
  "version": "1.0.0",
  "description": "gna energy backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate_models": "sequelize-auto -h localhost -d selfproject -u root -x root -p 3306  --dialect mysql  -o ./models"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.5",
    "sequelize": "^6.35.2",
    "sequelize-auto": "^0.8.8"
  }
}
